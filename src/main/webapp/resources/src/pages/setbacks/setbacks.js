define(function(require) {
    var baseUrl = require('../../common/js/baseUrl/baseUrl');
    return function(app) {
        app.controller('setbacksControl', ['$scope', '$http', function($scope, $http) {

        }])
    }
})
